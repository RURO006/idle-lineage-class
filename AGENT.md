# AI 更新規範

本檔案是本專案提供給 AI agent 的維護指引。

## 版本號

- 每次更新專案時都必須新增版號，包含功能新增、錯誤修正、平衡調整與重要文件更新。
- 版號唯一來源是 [js/00-data.js](js/00-data.js) 的 `GAME_VERSION`：

  ```js
  const GAME_VERSION = '版號';
  ```

- 修改內容時，請同步更新這一行的版號；目前採用 `v3.8.x` 格式，末段由 `0` 至 `99` 線性遞增，達到 `100` 時中位數加一、末段歸零。
- 完成更新前，確認程式碼中的 `GAME_VERSION` 與 README 最新更新紀錄的版號一致。

## README 更新紀錄

- 每次更新都必須同步修改 `README.md`，以繁體中文簡要記錄更新內容。
- 最新紀錄放在最上方，至少包含「更新時間」、「版號」與「更新內容」三欄。
- README 不記錄 Commit hash；更新時間使用 `YYYY-MM-DD` 格式即可。
- 若一次更新包含多項變更，請在同一筆紀錄中完整整理，避免只記錄其中一項。
